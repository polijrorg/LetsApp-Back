model User {
  id         String   @id @default(uuid())
  name       String?
  phone      String   @unique
  email      String?  @unique
  photo      String?
  code       Int?
  created_at DateTime @default(now())
  updated_at DateTime @default(now())

  @@map("user")
}
